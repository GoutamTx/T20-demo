apiVersion: v1
kind: Pod
metadata:
  name: attacker-setns
spec:
  containers:
  - name: alpine
    image: alpine
    command: ["sleep", "3600"]
    securityContext:
      privileged: true
  hostPID: true
